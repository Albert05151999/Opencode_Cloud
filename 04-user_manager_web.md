# 本地聊天与 Agent 配置管理平台

本文为 04 阶段的功能与实现清单，替代此前草案。实施状态以文末任务记录和验收产物为准，不以页面存在代替真实运行验证。

当前补充（2026-09-12）：本阶段能力已实现，文末 0.2.0 记录是当时验收结果。后续已增加配置 JSON 预览、Agent 归档/恢复/永久删除、沙箱运维和统一资源导入导出（[05 阶段](05-management_operations_plan.md)），以及自动压测与容量检查（[06 阶段](06-load_testing_todolist.md)）。Windows 自动模型发现仍为模型专用入口；统一导入入口另行支持原生模型/MCP/Skills，先进入全局资源池再显式发布分配。当前交付状态见 [项目进度](项目进度.md)。

## 1. 确定的产品规则

Windows localhost 应用，个人单管理员，React/TypeScript/Vite 页面由本地 FastAPI 服务提供，默认 `127.0.0.1:18765`。服务器默认 `http://106.52.221.61:18080`，首版 HTTP + Bearer 凭据，明确显示连接未加密。日常操作全部使用公开 API，不依赖 SSH。

- 模型全局保存，经 LiteLLM 调用；Agent 显式选择 allowed/default/small model。新模型不自动分配，提供商密钥不下发沙箱。
- MCP、Skills、Hook 全局仅作资源库；Agent 固定引用已发布版本，也可以创建私有资源。资源发布不自动更新引用者。
- Agent 支持创建、复制、编辑、指令和停用。复制不复制会话和工作区；停用保留历史，不强制取消已有任务。
- MCP 支持远程 URL/headers 和沙箱已有命令/参数/cwd/environment，不自动安装依赖，不支持 OAuth。
- Skills 支持单个 SKILL.md 和一个 Skill 的 ZIP，保留辅助文件，使用原生 skills.paths 按需加载，不拼接所有 Skill 全文。
- Hook 是原生 JS/MJS/TS 插件，在线编辑保存草稿、校验、发布、排序；只在沙箱执行，保留受保护的系统 trace 插件。
- Windows 扫描常见 OpenCode JSON/JSONC、指定目录、环境变量/认证文件引用；仅导入模型信息，其他扩展走独立管理流程。

## 2. 页面

### /chat

简约会话侧栏、中央对话、底部输入框，明暗主题与窄屏适配。支持 Agent/模型选择、会话新建/历史/搜索/重命名/删除、多轮 SSE、Markdown/代码复制、工具详情、停止、权限和问题回复、附件上传、产物下载。刷新补查历史，断线不自动重复提交请求。显示 Agent 当前配置版本和启用扩展摘要。聊天附件不安装 Skill。

### /admin

连接：服务器、凭据、能力和就绪状态。

全局资源：模型导入/预览/测试/启停/引用关系；MCP 表单；Skill 上传与文件树；Hook 编辑器；资源版本、草稿、发布与归档。

Agent：稳定 ID、名称、说明、指令、启停、模型分配、全局版本引用、私有资源、Hook 顺序、生效配置、发布历史及回滚。不提供任意 Docker 参数、镜像或宿主机挂载编辑。

诊断：发布任务、MCP 连接、Hook 加载、脱敏错误，不提供任意宿主机文件与日志入口。

## 3. 发布与数据规则

全局模型引用必须完整：default/small 属于 allowed；被 Agent 引用的模型禁用/删除前须替换引用。模型限制在服务端执行。

资源版本不可变；引用旧版的 Agent 不因全局编辑而变化。可复制全局资源为私有资源；同 Agent 中不允许重名 MCP/Skill 或重复 Hook。引用中的版本不能删除，可归档但不破坏引用。有效配置包含来源、版本与运行路径。

Skill ZIP 上限 20 MiB，解压上限 100 MiB，最多 1000 文件。拒绝穿越、绝对路径、链接、重复路径、多 Skill 根；验证 name/description，上传不执行文件。

MCP 表单字段：remote(name,url,headers,timeout,enabled)；local(name,command[],cwd,environment,timeout,enabled)。测试使用目标 Agent 的隔离沙箱，缺失依赖明确失败。

Hook 保存即服务器草稿持久化，发布后才影响运行。JS/TS 静态校验不得在控制器执行用户代码；沙箱加载测试，禁止自动 npm 安装；允许运行环境模块和同资源相对依赖。

发布生成不可变 Agent 目录。只排空受影响 Agent 的任务，等待最多 120 秒，超时保留候选版本不强制取消。SSE 连接不代表任务忙碌。模型网关修改单独发布；Agent 扩展不重启网关。失败回滚，重启恢复未完成任务，保留会话与工作区。

本地服务校验 Host/Origin/CSRF，仅代理已配置服务器，不开放任意文件读取。Windows Credential Manager 保存管理员凭据；浏览器不持久保存密钥。服务器的公开接口除最小存活检查外统一 Bearer 验证，平台 Authorization 不转发至沙箱。HTTP 明文传输是首版已确认边界。

## 4. API 分组

| 分组 | 能力 |
|---|---|
| /cloud/capabilities | 能力、版本与认证 |
| /cloud/models, /cloud/agents | 聊天目录 |
| /cloud/admin/models | 模型编辑、导入、测试与网关发布 |
| /cloud/admin/resources | 全局和 Agent 私有资源 |
| /cloud/admin/resources/{id}/versions | 资源草稿、版本、文件、源码 |
| /cloud/admin/agents | 创建、复制、编辑、停用 |
| /cloud/admin/agents/{id}/bindings | 模型、资源与顺序 |
| /cloud/admin/agents/{id}/effective-config | 脱敏生效配置 |
| /cloud/admin/agents/{id}/apply, /rollback | 发布和恢复 |
| /cloud/admin/jobs/{id} | 异步任务 |

管理接口使用资源 ID 与 DTO，不接受任意服务器写入路径。原生 /config 不是平台配置发布入口。新 API 纳入 OpenAPI 和 curl 示例。

## 5. TODO 与验收记录

- [x] 04-01 固化页面流程、接口及兼容边界。
- [x] 04-02 模型、资源版本、Agent 引用、有效配置、发布任务存储。
- [x] 04-03 单管理员认证与旧客户端支持。
- [x] 04-04 全局模型、Agent 分配与服务端校验。
- [x] 04-05 全局/私有资源库、固定版本与引用关系。
- [x] 04-06 Agent 创建、复制、停用、指令。
- [x] 04-07 配置编译、预览、不可变目录。
- [x] 04-08 MCP 校验、秘密字段与隔离测试。
- [x] 04-09 Skill 上传、解包、文件树与版本。
- [x] 04-10 原生 Skills 加载与示例迁移。
- [x] 04-11 Hook 源码、编译、测试、排序。
- [x] 04-12 发布、任务排空、回滚与恢复。
- [x] 04-13 本地同源服务、前端、一键启动。
- [x] 04-14 连接、凭据、能力与流式代理。
- [x] 04-15 Windows 配置发现、JSONC 和凭据引用。
- [x] 04-16 模型转换、预览、导入和分配。
- [x] 04-17 全局库和 Agent 管理页面。
- [x] 04-18 MCP 表单、Skill 上传、Hook 编辑器。
- [x] 04-19 更新提示、配置预览、发布与回滚页面。
- [x] 04-20 聊天布局、会话和模型选择。
- [x] 04-21 SSE 去重、恢复、停止与重试。
- [x] 04-22 工具、权限、问题、附件、产物。
- [x] 04-23 模型导入到远端对话真实验证。
- [x] 04-24 MCP、Skill、Hook 真实调用及隔离验证。
- [x] 04-25 迁移、发布失败、重启恢复测试。
- [x] 04-26 本地包、升级包、部署说明、验收报告。

验收必须覆盖未绑定模型拒绝、全局资源不自动加载、固定版本不漂移、私有资源隔离、停用保留历史、非法 ZIP、Hook 语法错误、MCP 失败诊断、运行任务不中断、重启回滚、SSE 不串流不重复提交、附件与权限闭环、脱敏、旧数据与 CLI 兼容。模拟与真实模型证据分别记录。

不包含：多用户、WSL 扫描、HTTPS 部署、MCP OAuth、依赖自动安装、任意 npm 插件、宿主机 Hook、无校验覆盖 OpenCode 配置。

参考：[MCP](https://opencode.ai/docs/mcp-servers/)、[Skills](https://opencode.ai/docs/skills/)、[Plugins](https://opencode.ai/docs/plugins/)。运行契约以固定 OpenCode 1.18.29 的本地 OpenAPI 和镜像验证为准。

## 6. 实施与验证记录

结果：0.2.0 实现及验证通过。公开服务器尚未自动升级，执行部署说明中的上传和安装命令后即可连接。

| 任务 | 依赖（04-） | 产物 | 验证 | 结果 |
|---|---|---|---|---|
| 04-01 | — | 04 文档、API 契约、OpenAPI | V、W | 通过 |
| 04-02 | 01 | management.py SQLite、版本、草稿与任务 | U | 通过 |
| 04-03 | 02 | Bearer 中间件、SSE CLI、Windows 凭据 | U、R、W | 通过 |
| 04-04 | 02/03 | 模型存储、分配、请求服务端校验 | U、G、W | 通过 |
| 04-05 | 02/03 | 资源归属、不可变版本、引用查询 | U、R | 通过 |
| 04-06 | 04/05 | Agent 新建、复制、停用与指令 | U、G | 通过 |
| 04-07 | 04/05/06 | 编译目录、当前/草稿预览及运行路径 | U、R、G | 通过 |
| 04-08 | 05/07 | remote/local MCP、cwd 适配与独立探测 | U、R | 通过 |
| 04-09 | 05 | Skill MD/ZIP 校验、文件树与版本 | U、R | 通过 |
| 04-10 | 07/09 | skills.paths 原生发现；发布时移除重复注入 | U、R | 通过 |
| 04-11 | 05/07 | Hook 源码、静态编译、沙箱加载和顺序 | R | 通过 |
| 04-12 | 07/08/10/11 | 排空、发布、失败保留、重启恢复 | U、G、W | 通过 |
| 04-13 | 01 | 本地 FastAPI、Vite 编译页面、start-web.ps1 | V、W | 通过 |
| 04-14 | 03/13 | 连接、凭据保险库、能力、同源 SSE 代理 | W | 通过 |
| 04-15 | 13 | Windows 发现、文件选择、JSONC/env/file/auth | U、W | 通过 |
| 04-16 | 04/14/15 | 四种模型适配、来源/默认值预览、导入/分配 | U、G、W | 通过 |
| 04-17 | 05/06/13 | 全局库和 Agent 编辑页 | V、W | 通过 |
| 04-18 | 08/09/11/17 | MCP 表单、Skill 附件、Hook 编辑器 | V、R | 通过 |
| 04-19 | 12/17 | 固定版本更新提示、预览、任务、回滚 | V、R、G | 通过 |
| 04-20 | 13/14 | 聊天布局、会话、Agent/模型、明暗和窄屏 | V、W | 通过 |
| 04-21 | 20 | SSE 消息合并、刷新恢复、停止、手动重试 | U、V、W | 通过 |
| 04-22 | 20/21 | 工具详情、权限/问题回复、聊天文件 | V、W | 通过 |
| 04-23 | 16/20/21 | Windows 配置 → 网关 → Agent → SSE | W | 通过 |
| 04-24 | 08/10/11/20 | 两类 MCP、Skill、Hook 实际调用与顺序 | U、R | 通过 |
| 04-25 | 12/23/24 | 0.1.0 会话迁移、发布失败、恢复与在途任务 | U、G、W | 通过 |
| 04-26 | 01–25 | 本地包、离线镜像包、部署命令、验收报告 | P | 通过 |

验证代号、精确命令、真实与模拟边界见 [验收报告](artifacts/web/final-assessment.md)。P 的最终 ZIP 哈希和完整性以 `artifacts/release/web-v0.2.0/package-verification.json` 为准。
