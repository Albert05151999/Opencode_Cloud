# Generate a strong token; use the same token for internal services.
SERVICE_TOKEN=

# Explicit seed-import convenience values. ADMIN_TOKEN authenticates requests
# sent through the public API; model values are substituted only with --apply.
ADMIN_TOKEN=
SEED_MODEL_BASE_URL=
SEED_MODEL_NAME=
SEED_MODEL_API_KEY=
