# 05：管理与运维能力实施计划

状态更新：2026-09-12。0.3.0 管理运维功能、原批次打包及隔离部署验收完成；05-16 生产部署仍无新验收证据，上次尝试受 SSH 登录条件阻塞。基线：0.2.2。后续压测及容量检查已完成源码验证，见 [06 阶段](06-load_testing_todolist.md)，不包含在本文原批次 ZIP 中。当前总览见 [项目进度](项目进度.md)。

本计划承接 `04-user_manager_web.md`，补齐管理可用性，不重做聊天平台或鉴权体系。用户授权按推荐默认方案推进；以下默认值属于本次规划选择，不代表已上线行为。

## 1. 目标与默认决策

- Windows localhost Web 通过公开 API 完成 Agent 生命周期管理、沙箱运维、资源导入导出和模型配置。
- 外围只传递经过校验的配置与稳定 ID，不关心 Docker、数据库、宿主机路径及原生配置生成细节。
- 继续使用 LiteLLM、固定 OpenCode 版本与 SQLite，不增加数据库服务、消息队列或任意插件执行框架。
- Agent 默认归档、可恢复；永久删除是独立操作。未发布且没有关联数据的空 Agent 可以直接删除。
- 自动恢复默认开启，仅处理确认为无活跃任务的异常沙箱；忙碌或无法确认任务状态时告警，不自动强制重启。
- 导出提供脱敏配置包和口令加密的配置迁移包。迁移包包含配置、资源文件、引用及可选凭据；首版不包含聊天记录、工作区和容器磁盘，不将其称为整个平台灾备。
- 所有外部导入首先成为全局资源池草稿，不自动绑定或发布 Agent。已有 Agent 私有资源仍可保留和编辑。
- 常规厂商模板优先覆盖 OpenAI、Anthropic、Google，以及通过 OpenAI-compatible 适配的 DeepSeek、通义千问、智谱和 MiniMax；具体端点、模型 ID 在实施时查证官方说明，不能把未经验证的示例标记为通过。

## 2. 实体与不变量

| 实体 | 身份与关系 | 不变量 |
|---|---|---|
| 平台 Agent | 稳定 agent_id，一组配置及发布历史 | 不等于 OpenCode 内部子 Agent；复制不复制会话与工作区 |
| 沙箱 | 稳定 sandbox_id，对应 agent_id + username | 容器重建后 container_id 可变；运维以 sandbox_id 寻址 |
| 会话 | session_id，属于沙箱并有独立目录 | 一个沙箱可承载多个会话；重启影响整个沙箱 |
| 全局模型 | 平台 model_id，映射厂商 upstream_model | Agent 引用平台 ID；真实模型凭据由网关使用 |
| 扩展资源 | resource_id、kind、归属、不可变发布版本 | 全局发布不自动更新已有 Agent 固定版本绑定 |
| 运维任务 | job_id、作用范围、状态、操作代次 | 重试和控制器恢复不能重复执行破坏性动作 |

模型仍采用现有全局网关版本语义：修改模型并发布网关会影响引用该模型的 Agent。不要把扩展资源固定版本语义误套到模型；页面必须显示影响范围。

## 3. 封装与扩展边界

逐步从现有 `management.py`、`management_runtime.py`、`admin_api.py` 和 `Admin.tsx` 拆分，保持旧调用入口作为兼容门面，避免一次性重写。

| 模块 | 责任 | 不允许承担的职责 |
|---|---|---|
| lifecycle service | Agent 状态、归档、恢复、删除影响分析 | 直接拼 Docker 参数或在路由内删文件 |
| sandbox operations service | 查询、停止、启动、重启、任务排空 | 独立绕过发布锁、重复实现自动恢复 |
| recovery policy | 根据观测与策略决定是否创建恢复任务 | 在健康检查回调中直接重建容器 |
| job coordinator | 幂等、作用范围互斥、持久状态、恢复与进度 | 执行模型请求或自动重放聊天消息 |
| resource transfer service | 导入预览、冲突、提交、导出、引用重映射 | 绕过资源校验器直接写生效文件 |
| source adapters | 将原生配置、目录、ZIP 转为标准导入项目 | 保存资源、发布或改变 Agent |
| provider adapters | 字段能力、参数校验、LiteLLM 编译、探测 | 混入 React 状态与表单组件 |
| provider templates | 推荐端点、字段帮助、示例及能力提示 | 承载可执行代码或厂商真实凭据 |
| repositories / blob store | SQLite 读写和资源内容寻址 | 决定业务生命周期或重启策略 |
| API / Web | DTO、展示、传递用户意图 | 直接调用 Docker 或了解宿主机目录结构 |

模板支持声明式字段、默认值、条件展示和参数白名单，使用固定组件注册表。兼容厂商只新增模板；协议不同才增加适配器及契约测试。不建立通用动态代码插件系统。

## 4. 统一任务执行与状态

任务类型：agent.archive、agent.restore、agent.delete、sandbox.start/stop/restart/recover、resource.import/export、model.test，以及现有发布任务。

任务状态：queued → validating → waiting → applying → succeeded；分支为 failed / cancelled。任务保存请求 ID、目标、配置版本、进度、错误码、结果和恢复检查点。只有尚未进入不可逆阶段的任务可以取消。

- API 对写操作接受幂等键和预期 revision；重复请求返回同一个任务，不重复执行。
- 锁定层次为平台/网关 → Agent → 沙箱，固定顺序获取；Agent 发布、归档与其沙箱运维不能交错执行。
- 初期仍为单控制器，但互斥状态持久化并携带操作代次，不能仅依赖进程内集合。
- 外部操作不在长 SQLite 写事务中执行；短事务提交任务意图，再执行 Docker/文件操作，再记录结果。
- 阻止新生成后，结合请求计数与各会话原生执行状态判断排空；SSE 打开不代表任务忙，探测失败不代表空闲。
- 默认等待 120 秒；超时返回可重试结果，不强制中断。管理员显式强制操作时显示受影响会话，记录中断。
- 控制器重启后按检查点核对实际容器和文件状态，再继续或失败；禁止盲目重复 remove/restart/import。
- 沙箱恢复绝不自动重复提交历史模型请求，UI 显示执行中断及手动重试入口。

## 5. Agent 生命周期

生命周期字段独立于草稿 enabled：active、archiving、archived、deleting。页面分别显示生命周期、当前已发布启用状态、草稿差异和关联沙箱数量。

归档：立即阻止新生成，等待运行任务排空，停止关联沙箱，保留配置、版本、会话和工作区，从默认列表隐藏。失败时显示原因与任务状态。恢复：恢复归档前的已发布启用状态，不把未发布草稿偷偷生效；沙箱按请求启动。

删除使用两步协议：获取影响预览 → 携带预览令牌和 revision 确认。预览列出关联沙箱、会话/文件计数、私有资源、历史版本及是否有活跃任务；过期或依赖变动后要求刷新预览。

永久删除首版只允许已归档且无活跃任务的 Agent，删除所属会话、工作区、私有资源和配置历史；全局资源不随 Agent 删除。删除任务保留最小结果与 ID 墓碑，避免旧会话 URL 指向新实体。共享 blob 通过引用计数/可达性扫描回收，不按目录归属猜测。

空 Agent 直接删除仅适用于从未发布、无沙箱、无会话且无私有资源的对象。发布历史不为空时走归档流程。

## 6. 沙箱管理与异常恢复

列表提供分页、按 sandbox_id/container_id/agent_id/username 搜索及状态筛选；详情显示配置版本、容器状态、OpenCode 健康状态、任务状态、CPU/内存快照、最后活动、异常原因与操作记录。默认 5 秒刷新，可手动刷新，后台页面停止高频轮询。

运行状态与健康状态独立：running 不等于 healthy。统计指标带采样时间和未知状态；不将所有日志或文件暴露为诊断页面。

手动启动/停止/重启共享统一运维服务。停止记录 desired_state=stopped；自动恢复不得拉起人工停止、归档 Agent、空闲回收或发布中的沙箱。强制重启是独立显式选项，不默认启用。

默认恢复策略：连续 3 次探测失败；确认无活跃任务后尝试重启，失败再受控重建；30/60/120 秒退避，15 分钟窗口最多 3 次，成功稳定 5 分钟后重置。达到上限标记需人工处理；宿主机 Docker 或网关整体故障时暂停批量恢复，避免所有沙箱同时重建。默认并发恢复上限 2。

策略通过管理设置保存，服务器校验后生效；示意 DTO（不是现有接口）：

```json
{
  "enabled": true,
  "failure_threshold": 3,
  "busy_action": "alert",
  "max_attempts": 3,
  "window_seconds": 900,
  "backoff_seconds": [30, 60, 120],
  "drain_timeout_seconds": 120,
  "max_parallel_recoveries": 2
}
```

先实现全局策略及每个 Agent 的 enabled 开关，不首发任意多层策略覆盖。管理员无需编辑 Docker 配置。

## 7. 统一导入与导出

标准流程：选择来源 → 发现资源及实际附件 → 解析预览 → 解决冲突 → 原子写入全局草稿池 → 校验/发布 → 显式分配 Agent。

### 来源适配

- 原生 JSON/JSONC：provider/models、mcp、skills 路径声明；model/small_model 仅作为建议。
- 模型：沿用 env/file/auth 文件解析，显示支持字段与拒绝原因。
- MCP：逐条导入 local/remote；命令在远端沙箱执行，Windows 命令、盘符、相对路径及缺失依赖必须列为待处理项，不能机械搬运。
- Skills：单文件、单 Skill ZIP、明确选择的原生 Skill 目录或多资源包；读取路径声明时需要包含实际 SKILL.md 与辅助文件。远端不能读取 Windows 路径。
- 目录发现范围有限，不能自动全盘扫描；聊天附件不参与资源安装。
- 首次统一原生导入聚焦 model/mcp/skills；Hook 继续编辑/上传流程，并纳入平台迁移包。

预览项目包含来源定位、类型、目标 ID、名称、摘要、依赖、差异、警告和错误。用户逐项选择跳过、重命名或新建版本；默认不覆盖。未知字段不能静默丢弃，必须拒绝或由用户明确排除。

提交以预览 ID、来源摘要、revision 和冲突决策为输入；过期后重新预览。按一次选择事务提交草稿元数据；先暂存附件，失败时没有半份资源对外可见，暂存 blob 可回收。同名不同内容不得误判为相同资源。

所有外部来源（包括迁移包中的私有资源）先映射为全局池草稿；原归属仅作来源信息。Agent 配置进入独立“待恢复模板”，不创建或覆盖生效 Agent；管理员确认分配后才能创建草稿和发布。

### 导出格式

1. 原生 OpenCode 配置包：opencode.json、选定 Skill 文件与相对路径；模型默认导出当前平台网关连接语义，明确依赖原服务器。独立厂商直连导出不在首版，避免误称无损转换。
2. 平台脱敏包：版本化 manifest、模型定义、MCP、Skills、Hook、内容摘要及可选 Agent 配置模板，秘密字段使用待填写占位，不覆盖目标已存秘密。
3. 加密配置迁移包：相同 manifest 加可选凭据，使用成熟密码库与版本化容器格式，口令派生、认证加密及参数上限在实现设计中固化；不自创算法，不保存用户导出口令。

包声明 format_version、最低兼容版本、资源 ID 映射、文件清单和校验摘要。旧版本通过显式迁移器导入，未来不支持的版本直接拒绝。首版配置包上限建议 200 MiB、总解包 500 MiB/10000 文件；单 Skill 仍保留 20 MiB/100 MiB/1000 文件限制。流式处理并限制任务并发，不能把整个包常驻控制器内存。

## 8. 模型配置与厂商说明

统一字段区分：平台 model_id、显示名称、厂商 upstream_model、协议适配器、厂商模板、端点、凭据与调用参数。厂商模板更新不改变已保存配置。

向导：选厂商 → 填凭据与模型 → 测试当前草稿 → 保存全局模型 → 发布网关 → 选择 Agent → 设置默认/小模型 → 发布 Agent。每步明确“已保存”“已发布”“已分配”；允许分步完成。

草稿测试通过隔离临时网关配置调用，复用真实编译逻辑，不修改正在运行的全局网关。已发布模型测试明确标记测试版本。错误区分 DNS/连接、端点、凭据、模型名、参数和超时，不能全部显示连接失败。

每个厂商示例包含表单值、生成的 LiteLLM 条目、Agent 引用片段、端点区域与模型名注意事项。JSON 预览复用服务端编译器，前端不再自行维护另一套关键转换规则。

## 9. 公开接口草案

保留现有接口；新接口均进入 OpenAPI，以专用 DTO 约束，不接受任意路径和 Docker 参数。分页游标、错误码、revision、任务返回格式统一。

| 接口组 | 操作 |
|---|---|
| `/cloud/admin/agents/{id}` | 详情、现有草稿编辑；delete-preview、archive、restore、delete 操作 |
| `/cloud/admin/sandboxes` | 分页查询与过滤；`/{id}` 详情；`/{id}/start/stop/restart` 异步操作 |
| `/cloud/admin/recovery-policy` | 读取、验证、保存全局策略 |
| `/cloud/admin/provider-templates` | 模板目录与表单能力；`/models/test-draft` 当前草稿测试 |
| `/cloud/admin/imports` | 创建上传/解析任务、预览、提交、取消 |
| `/cloud/admin/exports` | 创建导出任务、获取结果文件 |
| `/cloud/admin/jobs/{id}` | 状态、结果、可取消阶段及结构化错误 |
| 本地 `/local/imports` | Windows 发现与选择、凭据引用解析、向服务器提交资源文件 |

资源池的创建接口和已有私有资源编辑保持兼容；统一外部导入接口始终要求目标为全局池。导入 Agent 模板的后续分配是独立动作。

## 10. 迁移、页面与交付

- SQLite 增加 schema_version 和逐版迁移；迁移前备份，失败不启动新运行逻辑。现有 enabled、active/version、资源 owner 和绑定保持原含义。
- 沙箱增加 desired_state、operation_generation、恢复计数与 last_error；既有容器先对账，不能因缺省字段触发批量重启。
- 发布/运维历史独立分页，避免全局 catalog 每次返回全部文件内容和历史导致管理页越来越慢。
- 前端拆分 Agent、Sandbox、ResourceTransfer、ModelWizard 页面及共享 JobProgress、JsonPreview；表单切换前保护未保存内容。
- 能力探测支持旧服务器：不支持的新入口显示升级说明，不能出现无解释的 404。
- 交付服务器包、Windows Web 包、原生与平台导入示例、部署指南、迁移说明和验收报告；版本号在实现完成时确定，不提前覆盖 0.2.2 交付物。

## 11. 实施 TODO 与验收

所有项目初始未完成。每项完成时记录代码、命令、结果、真实与模拟边界。不得用规划文字或仅有 DTO 代替功能验收。

| 编号 | 依赖 | 产物 | 核心验收 |
|---|---|---|---|
| [x] 05-01 | — | 实体、状态、DTO、迁移骨架 | 0.2.2 数据迁移保留历史、引用及工作区 |
| [x] 05-02 | 01 | JobCoordinator、作用范围锁、幂等 | 双重点击、并发发布与重启不重复执行；进程中断可恢复 |
| [x] 05-03 | 01 | 沙箱查询服务与分页 API | 多会话归属、ID 搜索、状态未知与采样时间正确 |
| [x] 05-04 | 02/03 | 沙箱启动/停止/重启 | 忙碌排空、超时、强制确认、人工停止保持；不重发模型请求 |
| [x] 05-05 | 04 | 自动恢复策略 | 空闲异常恢复、忙碌跳过、退避上限、整体故障抑制 |
| [x] 05-06 | 02/04 | Agent 归档、恢复、删除 | 删除预览过期保护；全局资源不删除；共享文件引用完整 |
| [x] 05-07 | 03–06 | 沙箱和 Agent 页面 | 搜索到指定沙箱并重启；归档筛选和影响范围清楚 |
| [x] 05-08 | 01/02 | 标准导入项目、暂存和冲突事务 | 解析不落正式资源；提交失败无半导入；同源重复导入可解释 |
| [x] 05-09 | 08 | 原生模型/MCP 解析器 | JSONC/env/file/auth；不支持项明确；Windows 命令迁移提示 |
| [x] 05-10 | 08 | Skills 目录/包导入 | 文件完整、原生发现可用、原路径不跨机器照搬 |
| [x] 05-11 | 08–10 | 原生/平台脱敏导出 | 导出再导入语义一致；缺失凭据不覆盖已有值 |
| [x] 05-12 | 11 | 加密配置迁移包 | 正确口令恢复、错误口令/损坏包失败、资源 ID 冲突重映射 |
| [x] 05-13 | 09–12 | 导入导出向导与分配 | 所有导入先进入全局草稿池，无自动绑定或发布 |
| [x] 05-14 | 01/02 | 厂商模板、适配器、草稿测试 | 新兼容厂商仅加模板；草稿测试不改全局网关 |
| [x] 05-15 | 13/14 | 模型向导与厂商示例 | 平台 ID 到厂商模型映射可见；Agent 调用符合分配 |
| [ ] 05-16 | 全部 | 回归、打包、部署与验收 | 干净 clone 构建，Windows UI 连远端，真实模型/MCP/Skill 调用 |

实施顺序：01 → 02 → 03/04/05 → 06/07 → 08/09/10 → 11/12/13 → 14/15 → 16。不新增自动多 Agent 开发安排。

重点验证采用状态机与故障注入测试、API 契约测试、浏览器端到端测试及真实 Docker 集成测试。新操作分别覆盖排空中、Docker 完成但任务未落库、导入文件暂存后等崩溃窗口。真实厂商测试需要可用配置，缺少时列为未验证，不伪造通过。

## 12. 本轮不做

多用户权限体系、分布式调度、多宿主机沙箱、自动依赖安装、任意插件协议、WSL 全盘扫描、任意宿主机路径编辑、自动重放被中断会话，以及整个平台数据灾备。后续可以在上述服务边界扩展，不为这些需求提前建立复杂框架。

## 05 管理运维：第一批实现进度（2026-09-10）

状态：开发中，未发布新版本。05-01～05-07 只完成了以下子集，不能将整项验收标记为通过。

- 新增 Operations 服务与独立 API 路由；复用发布互斥锁、Agent 范围排空、任务记录与幂等请求 ID。
- 沙箱列表支持 ID/容器/Agent/用户搜索、状态过滤和分页；管理页支持启动、停止、重启及任务反馈。
- 手动停止意图持久化；聊天获取沙箱时拒绝自动唤醒人工停止的沙箱。
- Agent 支持归档、恢复及删除没有发布历史、沙箱和私有资源的空 Agent；删除后保留 ID 墓碑。
- 归档后新生成被拒绝，已保存配置不被修改。历史查询仍沿用现有原生接口，必要时会按需唤醒运行环境，不代表常驻运行。
- 控制器中断后将未完成操作标记为失败并要求检查实际状态，不盲目重复操作；尚未实现细分检查点恢复。

验证：Linux 原有及第一批新增测试共 224 项通过；随后补充的重启顺序、失败启动与编辑互斥测试，与其他运维用例组成的 10 项测试通过；TypeScript/Vite 构建通过；11 项 Playwright 测试通过，其中沙箱 UI 使用模拟 API。未执行生产沙箱重启或真实 Docker 运维验收。

第一批结束时的待办已由下方第二批记录更新。旧 0.2.2 ZIP 不包含这些源码改动。

## 第二批实现与验证（2026-09-10）

已落实的功能：

- `app/recovery.py` 封装自动恢复策略、持久计数与退避；使用任务状态证明空闲，新的请求会使旧空闲证据失效。策略和状态独立存表，不使目录配置版本持续变化。
- `app/agent_deletion.py` 提供带历史数据的删除预览和删除任务；预览过期/影响变化拒绝执行，失败保留删除中状态并允许重新预览重试。Agent ID 墓碑和 bootstrap 标记避免重启复活默认 Agent。
- 管理目录 schema 版本与迁移前 SQLite 备份；保留旧目录模型、Agent、版本和资源。
- `app/transfers.py` 负责统一预览、冲突处理、原子提交、凭据保留与全局草稿池语义；Windows 收集器处理指定原生配置及真实 Skill 目录。
- 平台模型/MCP/Skill/Hook 草稿导出、口令加密资源包、可选凭据导出；解密后走相同预览和确认流程，不自动发布或分配。
- 七个声明式厂商模板，当前表单隔离 LiteLLM 测试；UI 显示平台 ID 到上游模型的含义和发布、分配顺序。
- 页面增加归档筛选、永久删除影响确认、自动恢复设置、统一导入导出与厂商模板。操作说明见 `docs/management-operations.md`；API 契约已更新。

验证记录：

| 检查 | 命令 / 证据 | 结果与边界 |
|---|---|---|
| Linux 单元回归 | Python 3.12，`python -m pytest tests/unit` | 最终 250 项全部通过，包含删除中 Agent 发布/回滚保护 |
| 前端构建 | `cd web && npm run build` | 通过；仍有单个 JS bundle 大于 500 kB 提示 |
| 前端状态测试 | `cd web && npm test` | 9 项通过 |
| 浏览器 | Playwright 指向本机 18766 | 12 项通过；新厂商测试修正按钮定位后单独重跑通过，共 13 个用例。远端 API 为模拟 |
| 真实 Docker 运维 | `python scripts/verify-operations-runtime.py` | 通过：启动/停止/重启、幂等、会话文件保留、空闲异常自动恢复、归档/恢复、永久删除 |
| 真实模型扩展链路 | `python scripts/verify-management-runtime.py` | MCP 连接、Hook 静态/加载、Agent 发布及模型分配检查通过；真实生成请求 180 秒超时，整体验收未通过 |

真实 Docker 运维结果存于忽略目录 `artifacts/web/operations-verification.json`；脚本可重跑。测试清理自身唯一实例标签的容器，没有操作生产服务器。

第二批结束时仍缺少详情、强制操作预览、检查点、历史配置迁移和交付验收；以下第三批记录替代该时点的待办状态。

## 第三批实现与验证（2026-09-11）

05-01～05-15 的代码已实现。新增沙箱 CPU/内存及会话详情、强制停止/重启影响确认、持久操作检查点、分页任务与取消、Agent 自动恢复开关、原生配置 ZIP 导出、平台 v2 历史资源与 Agent 模板迁移、导入后显式发布/分配，以及旧服务器能力提示。模型表单 JSON 预览复用服务端编译器。

实现边界按本版代码收敛如下：

- 操作与发布共用串行锁，实际恢复并发为 1；不会超过策略上限。外围使用同一 DTO/API，后续可替换协调器实现。
- 中断恢复先检查实际容器及检查点，明确完成的操作直接记成功；无法确定或需要重新确认的强制操作记失败，不猜测重放。
- 删除 Agent 保留全局内容寻址资源文件，不实现共享 blob 垃圾回收，避免误删其他资源引用。
- 标准配置包 20 MiB；加密包 40 MiB、解密后 20 MiB。未采用 200 MiB 建议值。配置迁移不包含会话和工作区。
- 历史资源版本进入全局草稿，逐项发布后显式映射到待恢复 Agent 模板；不会把导入内容冒充已校验的运行版本。
- 七家厂商有模板与配置示例；真实调用验证覆盖现有 MiniMax、智谱配置，其他厂商没有账户调用证据。

| 验证 | 命令或产物 | 结果 |
|---|---|---|
| 后端回归 | `python -m pytest -q` | 最终 261 项通过，包含损坏模板输入 |
| 前端 | `cd web; npm run build; npm test` | 构建通过、9 项状态测试通过；单 bundle 体积提示保留 |
| 浏览器回归 | `npx playwright test`（18766） | 15 项通过，模拟远端 API |
| 真实模型与扩展 | `scripts/verify-management-runtime.py` | 模型分配、MCP、Skill、Hook 顺序、发布与运维全部通过 |
| 真实沙箱运维 | `scripts/verify-operations-runtime.py` | 重启/停止/启动、历史文件保留、恢复、删除、模板迁移通过 |
| 草稿模型调用 | `scripts/verify-draft-model.py` | 隔离 LiteLLM 真实请求通过，不改变正式网关 |
| 干净源码构建 | `scripts/verify-clean-source.py` | 不复制 `.env`、依赖目录及旧产物，完整构建与打包通过 |
| 最终控制器 | `scripts/verify-controller-image.py` | 启动、就绪、重启后原会话路由通过 |

05-16 的安装迁移、Windows 真流式连接和最终离线包结果见 `docs/release-0.3.0-assessment.md`。生产服务器 SSH 返回 `Permission denied (publickey,password)`；尚未部署到生产服务器，不能把隔离测试视为生产验收。

交付验证结果：0.2.2 → 0.3.0 迁移通过；Windows 凭据保存、导入、真实 SSE、工具、附件及产物下载通过；全新解压 Web 包的一键启动通过；独立 Docker 空镜像库安装、冒烟、重启和数据保留通过（共存试运行模式）。两份 ZIP 位于 `artifacts/release/web-v0.3.0/`，哈希见同目录 `package-verification.json`。
