# 项目进度

更新日期：2026-09-12。依据当前源码、05/06 阶段实施记录及已执行的验证结果整理；本次仅整理文档，没有重新部署生产服务器。

## 当前结论

版本文件仍为 **0.3.0**。聊天、配置管理与管理运维已有交付验收；随后新增的自动压测及服务器容量检查已完成源码实现和验证，**此前生成的 0.3.0 ZIP 不包含这些新增功能**。相同版本号不代表相同构建批次，请使用同批服务器包、Web 包和 `package-verification.json`。

## 已实现能力

| 模块 | 当前实现 | 详细说明 |
|---|---|---|
| 本地聊天 | Windows localhost Web、SSE、多轮历史恢复、停止、工具详情、权限/问题回复、附件与产物下载；思考内容首行折叠、文件手动及结束刷新、会话工作目录约束 | [04 阶段](04-user_manager_web.md)、[文件与目录修订](docs/ui-r3-files.md) |
| Agent 管理 | 创建、复制、编辑、启停用、归档与恢复、空草稿删除、影响预览后永久删除；复制不复制会话和工作区 | [管理运维](docs/management-operations.md) |
| 全局模型 | LiteLLM 统一网关；模型先进入全局配置，Agent 显式选择允许/default/small 模型，服务端校验分配；七家厂商模板与示例、草稿调用测试 | [模型示例](docs/provider-examples.md) |
| MCP / Skills / Hook | 全局或私有资源、固定发布版本引用；MCP 表单与隔离测试、Skill 文件/ZIP、Hook 在线编辑校验；草稿保存、发布与回滚 | [04 阶段](04-user_manager_web.md) |
| 配置可视化与迁移 | 全局/Agent 脱敏 JSON、表单对应 JSON；原生模型/MCP/Skills 导入、平台配置导出和加密迁移；导入先入资源池，再发布和分配 | [导入导出说明](docs/management-operations.md) |
| 沙箱运维 | 按沙箱/容器/Agent/用户标识搜索、状态与资源采样、启动/停止/重启、强制操作确认、空闲异常自动恢复、任务检查点对账 | [05 阶段](05-management_operations_plan.md) |
| 自动压测 | 选择现有 Agent、每 Agent 模拟用户数、每沙箱 CPU/内存；独立用户和沙箱同步发起固定会话；报告、取消与手动清理 | [压测说明](docs/load-testing.md) |
| 压测容量检查 | 实测 CPU、可用内存、现有配额、系统预留和可分配余量；5 秒刷新及手动刷新；超预算/未知状态拒绝启动；每批准备及生成前复查 | [06 阶段](06-load_testing_todolist.md) |

MCP 不自动安装依赖；原生插件迁移有明确边界，Hook 完整迁移使用平台格式。配置迁移不包含聊天历史、工作区和容器磁盘。全局资源发布不会自动更新已有 Agent 引用。

## 最近验证

以下是截至本次整理前已经执行的结果，不将历史发布包测试与最新源码测试混为一批：

| 范围 | 最近结果 | 证据入口 |
|---|---|---|
| 最新后端回归 | 286 项通过 | `python -m pytest -q`；[06 完成记录](06-load_testing_todolist.md) |
| 前端构建与逻辑 | 编译通过、9 项逻辑测试通过；仍有 bundle 体积提示 | `cd web` 后 `npm run build`、`npm test` |
| 浏览器回归 | 18 项通过，使用模拟远端 API | `web/e2e/workspace.spec.ts` |
| 最新真实压测 | Docker 容量采样、超预算无用户副作用、两用户真实模型请求、资源限制和清理通过 | `scripts/verify-load-testing.py`、本机 `artifacts/web/load-testing-verification.json` |
| 0.3.0 原交付 | 镜像与离线安装、0.2.2 迁移、Windows 真实 SSE、模型/MCP/Skill/Hook 等通过隔离验收 | [原交付报告](docs/release-0.3.0-assessment.md) |

验证产物在忽略目录 `artifacts/`，clone 不包含它们；验证脚本与文档保留在源码。真实容量样本来自开发验证宿主，不能当作公网服务器的当前剩余容量。

## 尚未完成的交付与边界

- 压测与容量检查需要重新构建服务器镜像包和 Web 包；本次未生成新的 ZIP、修改版本号或推送发布。
- 上一次生产部署尝试记录为 SSH 登录失败；此后没有新的生产部署验证证据。因此 05-16 的生产验收仍未完成，不推断服务器现状。
- 自动恢复与配置发布共用串行锁，实际恢复并发为 1；未知任务状态不自动强制重启。
- 压测预算是保守准入，不是系统资源预留；运行中的其他进程仍可能继续消耗内存。压测测服务端链路，不测浏览器、公网入口或首 Token 延迟。
- 七家厂商均有模板；真实账户调用记录只覆盖已验证厂商，不代表所有厂商均已实测。

## 文档阅读顺序

1. 使用与部署：[README](README.md) → [完整部署指南](部署指南.md) → [源码构建](docs/build-from-source.md)。已有包可按部署指南跳过构建，但应确认包含所需功能。
2. 管理操作：[管理运维](docs/management-operations.md)、[厂商示例](docs/provider-examples.md)、[压测](docs/load-testing.md)。
3. 实现契约：[架构](01_architecture.md)、[API](02_api_contract.md)。早期方案与保留的 JWT 扩展设计不等于当前已开放能力。
4. 阶段追溯：[03 基础阶段](03_zero_to_one_todolist.md) → [04 本地 Web](04-user_manager_web.md) → [05 管理运维](05-management_operations_plan.md) → [06 自动压测](06-load_testing_todolist.md)。历史验收数字保留原批次，不覆盖成最新数字。

`docs/progress.json` 保留基础阶段逐项记录，顶层 `latest_status` 指向当前阶段和本文件。
