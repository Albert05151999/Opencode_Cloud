# 从源码构建到首次部署：完整部署指南

更新说明（2026-09-12）：最新实现见 [项目进度](项目进度.md)。版本仍为 0.3.0，但已有历史包不含压测与容量检查。需要这些功能时从第 2 节用当前源码构建；已有输出目录时指定新的 `--output-dir`，同步修改第 4、6 节的 `$packageDir`。只有确认包含所需功能的同批两端包才可跳过构建。本文步骤不代表生产服务器已经更新。

本文默认：Windows 本机运行 Web，Ubuntu 服务器运行控制器、模型网关和 Agent；从源码构建使用 Windows 上的 Ubuntu 24.04 WSL（或独立 Linux 构建机）。本文不是升级指南，不包含停止旧版的步骤。

## 0. 应该从哪里开始

| 你目前的情况 | 从哪里继续 |
|---|---|
| 刚 clone，没有镜像包和 Web 包 | 第 1 节准备环境 → 第 2 节构建 |
| 已有服务器 ZIP、Web ZIP 和同批次 package-verification.json | 跳过构建，从第 3 节准备 `.env`，再按第 4 节上传 |
| 服务器已经部署且能访问，只需本地 Web | 跳到第 6 节；本机不需要服务器 `.env`，但需要服务器管理员令牌 |
| 服务器已部署，本地 Web 已解压或源码前端已构建 | 跳到第 8 节一键启动，然后按第 7 节连接 |
| 只有服务器镜像包，没有本地 Web 包 | 仍需获取对应版本 Web 包，或按第 2 节构建源码；服务器 ZIP 不包含本地 Web 应用 |
| 缺少 `.env` | 看第 3 节；构建不需要真实 `.env`，首次部署服务器需要 |

**终端约定：** 所有本地相对路径均从项目根目录执行。在资源管理器进入包含 `README.md`、`VERSION`、`deploy` 的目录，在地址栏输入 `powershell` 回车即可；使用编辑器终端时也确认工作目录为项目根目录。不要复制别人的盘符路径。远端命令只在 SSH 登录后的 Ubuntu 终端执行。

## 1. 环境与参数

| 位置 | 前置条件 |
|---|---|
| Windows 本机运行 Web | Python 3.11+，PowerShell；首次启动可联网安装 Python 依赖；上传时需要 `ssh`、`scp` |
| Linux / WSL 构建环境 | x86_64、Python 3.12 与 venv、Node.js 24 与 npm、Docker/Buildx、zstd、curl；可联网拉取镜像和依赖 |
| Ubuntu 服务器 | Docker Engine、Compose 插件、Python 3、zstd、unzip、curl，以及能使用 sudo 的 SSH 账户 |

构建验证要求 Docker 能使用该 Linux 环境的目录挂载。建议在独立的 Ubuntu 24.04 WSL 环境构建，不在生产服务器构建。项目默认严格部署检查要求 Docker ≥ 29.8.0、内存至少 8 GiB、可用内存至少 3 GiB、无已占用 swap，安装目录所在磁盘至少 8 GiB 空闲；解压 ZIP、镜像加载和备份还需要额外空间。以安装器实际检查结果为准。

Windows PowerShell 检查：

```powershell
python --version
ssh -V
Get-Command scp
```

本指南使用现有服务器作为示例；其他人部署时将 `ubuntu@106.52.221.61` 和服务器 URL 换成自己的地址。远端示例上传目录为 `/home/ubuntu`，更换 SSH 用户后也要同步更换该目录。安装目录为 `/srv/cloud-agent`，API 端口为 `18080`；本地 Web 端口为 `18765`。

## 2. 从源码构建两份安装包

**已有两份构建包的用户跳到第 3 节。**

在项目根目录打开 WSL 终端，例如从 PowerShell 执行 `wsl -d Ubuntu-24.04`。确认 `pwd` 对应当前项目后，在 **WSL Ubuntu** 执行：

```bash
sudo apt-get update
sudo apt-get install -y python3.12 python3.12-venv zstd curl xz-utils
python3.12 --version
docker info
docker compose version
node --version
npm --version
```

Docker / Compose 需要预先安装并启动。若 Node.js 尚未安装，可在项目根目录执行 `sudo bash scripts/install-node.sh`，安装项目固定的版本。`docker info` 必须成功，当前用户需要 Docker 权限。

前置条件满足后，一键构建：

```bash
bash scripts/build-from-source.sh
```

该脚本创建构建虚拟环境，安装依赖，构建前端，运行测试，构建并验证镜像，然后生成服务器离线包和本地 Web 包。首次执行需要下载依赖和镜像，请等待整个命令成功结束。

默认输出（版本取根目录 `VERSION`，当前为 0.3.0）：

```text
artifacts/release/web-v0.3.0/
  cloud-agent-release-0.3.0.zip
  cloud-agent-web-0.3.0.zip
  package-verification.json
```

如果默认输出目录已有旧构建结果，指定一个尚未使用的目录，例如：

```bash
bash scripts/build-from-source.sh --output-dir artifacts/release/source-build
```

之后第 4、6 节的 `$packageDir` 都改为 `'.\artifacts\release\source-build'`。每次重新打包的 SHA256 可能不同，本文直接从同批次报告读取，不写死旧哈希。若别人提供包，请一并索取 `package-verification.json`，不要为未知包自行生成一个“正确”校验值。

构建结束，在 WSL 输入 `exit`，回到项目根目录的 Windows PowerShell。进一步说明见 [源码构建说明](docs/build-from-source.md)。

## 3. 缺少 .env 时怎么办

`.env` 是服务器模型网关的私密配置，真实文件不提交 Git、不包含在 ZIP 中。**只运行本地 Web 或只构建镜像，不需要它。** 服务器已经部署且模型正常时，也不需要重新创建或上传。

首次部署，在项目根目录的 **Windows PowerShell** 执行：

```powershell
if (-not (Test-Path -LiteralPath '.\deploy\.env' -PathType Leaf)) {
    Copy-Item -LiteralPath '.\deploy\.env.example' -Destination '.\deploy\.env'
}
notepad .\deploy\.env
```

按模板填写并保存。默认模板需要填写 `ZAI_API_KEY` 和 `MINIMAX_API_KEY`；下方四组 `CODING_*`、`DATA_*` 模型通过 `${...}` 引用这些值。模板中的空白 key 不是可用凭据。

如果使用其他 OpenAI-compatible 提供商，需要同时修改相应四组槽位的 `API_KEY`、`MODEL`、`1_API_BASE`、`2_API_BASE`，使模型名、端点和凭据匹配，不能只替换 key 后保留不适用的模型地址。后续也可以通过管理页导入模型并分配给 Agent。

若只有服务器 ZIP、没有源码模板，可先将 ZIP 解压到临时目录，从其中 `deploy/.env.example` 复制出 `.env`，并在下一节将 `$envFile` 改为该文件的本地路径。不要覆盖已经填好的配置，也不要把 `.env` 内容发到聊天或公开日志中。

## 4. 本地校验并上传

**在项目根目录的 Windows PowerShell 执行。** 此代码块独立读取版本和哈希，不依赖构建终端的变量。

```powershell
$ErrorActionPreference = 'Stop'
$server = 'ubuntu@106.52.221.61'
$packageDir = '.\artifacts\release\web-v0.3.0'
$envFile = '.\deploy\.env'
$report = Get-Content -LiteralPath (Join-Path $packageDir 'package-verification.json') -Raw | ConvertFrom-Json
$version = $report.version
$releaseName = "cloud-agent-release-$version.zip"
$releaseZip = Join-Path $packageDir $releaseName
$entry = @($report.packages | Where-Object { $_.name -eq $releaseName })
if ($entry.Count -ne 1) { throw '报告中缺少唯一的服务器包记录' }
foreach ($file in @($releaseZip, $envFile)) {
    if (-not (Test-Path -LiteralPath $file -PathType Leaf)) { throw "找不到文件：$file" }
}
if ((Get-FileHash -LiteralPath $releaseZip -Algorithm SHA256).Hash -ne $entry[0].sha256) {
    throw '服务器 ZIP 与本次构建报告不一致'
}
# 将校验文件和 ZIP 一起上传，远端也会检查。
$checksumFile = Join-Path $packageDir 'server-upload.sha256'
Set-Content -LiteralPath $checksumFile -Encoding ascii -Value "$($entry[0].sha256)  $releaseName"
ssh $server 'umask 077; touch /home/ubuntu/.env; chmod 600 /home/ubuntu/.env'
if ($LASTEXITCODE -ne 0) { throw '准备上传失败' }
scp $releaseZip $envFile $checksumFile "${server}:/home/ubuntu/"
if ($LASTEXITCODE -ne 0) { throw '上传失败，请重试' }
Write-Host "已上传版本 $version，下一节使用这个版本号。"
ssh $server
```

最后一条命令会登录服务器。上传的 `.env` 仅放在 `/home/ubuntu`；首次安装会复制到安装目录。如果服务器已经部署，请直接跳到第 6 节，不用为了启动本地 Web 重装服务器。

## 5. 服务器首次部署并检查

以下在 **远端 Ubuntu SSH 终端** 执行。先检查基础工具，缺少 Docker/Compose 时应先安装并启动，再继续：

```bash
sudo apt-get update
sudo apt-get install -y python3 zstd unzip curl
sudo docker info
sudo docker compose version
```

执行整个代码块；`VERSION` 改为第 4 节显示的上传版本：

```bash
sudo bash <<'BASH'
set -euo pipefail
VERSION=0.3.0
cd /home/ubuntu
if [ -e /srv/cloud-agent/VERSION ]; then
  echo '检测到已部署实例。本文是首次部署流程；本地 Web 使用请直接跳到第 6 节。'
  exit 1
fi
chmod 600 .env
sha256sum -c server-upload.sha256
unzip -q "cloud-agent-release-${VERSION}.zip"
cd "cloud-agent-release-${VERSION}"
bash deploy/install.sh \
  --root /srv/cloud-agent \
  --env-file /home/ubuntu/.env \
  --host 0.0.0.0 \
  --port 18080
bash /srv/cloud-agent/deploy/status.sh --root /srv/cloud-agent
cat /srv/cloud-agent/VERSION
BASH
```

安装器加载镜像、准备配置并启动服务，无需再手动 `docker compose up`。遇到检查失败先处理具体错误，不要把失败当成部署完成。如果上次只完成了解压，可直接进入 `/home/ubuntu/cloud-agent-release-<版本>` 执行安装命令；若已经生成安装实例，先检查其状态。

验证管理员接口和就绪状态：

```bash
bash <<'BASH'
set -euo pipefail
TOKEN=$(sudo cat /srv/cloud-agent/data/admin-token)
for route in capabilities health/ready; do
  curl --noproxy '*' -fsS --max-time 30 \
    -H "Authorization: Bearer $TOKEN" \
    "http://127.0.0.1:18080/cloud/$route"
  printf '\n'
done
unset TOKEN
BASH
```

接着读取平台管理员令牌，保存供第 7 节填写：

```bash
sudo cat /srv/cloud-agent/data/admin-token
```

这是平台管理员凭据，**不是模型 API key**。不要把输出公开。已经部署的服务器也可以使用这条命令读取原令牌。

最后退出远端：

```bash
exit
```

服务在服务器后台运行，退出 SSH 不会停止它。API 监听 `0.0.0.0:18080`；公网使用还要求端口映射、防火墙及云安全组允许访问，或已有反向代理转发到该 API。`0.0.0.0` 不是浏览器访问地址，也不会绕过入站规则。

在 **本地 PowerShell** 检查网络：

```powershell
Test-NetConnection 106.52.221.61 -Port 18080
```

## 6. 本地解压 Web 包并启动

**服务器已经部署的用户从这里开始。** 无需本地 `.env`，也无需再次上传服务器包。

确认本机 `python --version` 为 3.11 或以上。在项目根目录的 **Windows PowerShell** 执行；如果包存放在其他地方，修改 `$packageDir`。旧 Web 正在运行时，先在其终端按 `Ctrl+C` 释放端口。

```powershell
$ErrorActionPreference = 'Stop'
$packageDir = '.\artifacts\release\web-v0.3.0'
$report = Get-Content -LiteralPath (Join-Path $packageDir 'package-verification.json') -Raw | ConvertFrom-Json
$version = $report.version
$webName = "cloud-agent-web-$version.zip"
$webZip = Join-Path $packageDir $webName
$entry = @($report.packages | Where-Object { $_.name -eq $webName })
if ($entry.Count -ne 1) { throw '报告中缺少唯一的 Web 包记录' }
if (-not (Test-Path -LiteralPath $webZip -PathType Leaf)) { throw "找不到 Web 包：$webZip" }
if ((Get-FileHash -LiteralPath $webZip -Algorithm SHA256).Hash -ne $entry[0].sha256) {
    throw 'Web ZIP 与本次构建报告不一致'
}
$webInstallRoot = Join-Path $env:LOCALAPPDATA 'OpenCodeCloud'
New-Item -ItemType Directory -Path $webInstallRoot -Force | Out-Null
Expand-Archive -LiteralPath $webZip -DestinationPath $webInstallRoot -Force
Set-Location (Join-Path $webInstallRoot "cloud-agent-web-$version")
powershell -ExecutionPolicy Bypass -File .\start-web.ps1
```

启动器创建独立 Python 环境、安装依赖并打开浏览器。保持终端运行；关闭或按 `Ctrl+C` 会停止本地 Web。发布包包含前端，**无需 Node.js**。如果浏览器没有自动打开，手动访问：

- 聊天页：`http://127.0.0.1:18765/chat`
- 管理页：`http://127.0.0.1:18765/admin`

如果你已经从源码完成第 2 节构建，也可以不解压 Web ZIP，直接在 Windows 项目根目录运行第 8 节命令，使用已生成的 `web/dist`。

## 7. 连接服务器并完成首次使用

在本地管理页进入服务器连接设置：

| 字段 | 填写内容 |
|---|---|
| 服务器 URL | `http://106.52.221.61:18080`，或你自己的服务器 API/反向代理入口 |
| 管理员凭据 | 第 5 节读取的 `data/admin-token` |
| 保存凭据 | 可勾选 Windows Credential Manager，方便以后启动 |

保存并测试连接，确认就绪后进入聊天页，选择 Agent 和模型并发送消息。服务器 URL 不要填 `ubuntu@...`、`0.0.0.0`、localhost Web 地址或 `/chat` 页面路径。HTTP 连接会显示明文传输提示。

如果需要导入本机 OpenCode 模型：全局模型中选择/发现配置 → 检查并导入 → 发布模型网关 → 在目标 Agent 勾选模型并设默认模型 → 发布 Agent。全局资源不会自动分配给所有 Agent；MCP、Skill、Hook 同样需要绑定并发布。

管理页顶部“全局 JSON”查看资源库和网关配置；Agent 的“配置预览与版本历史”查看已生效与草稿 `opencode.json`；表单下方显示对应 JSON。预览不会自动保存或发布。

完成一次会话后，可让 Agent 写入 `outputs/check.txt`，打开文件面板进入 outputs 下载，确认流式会话和文件链路可用。任务结束后面板会刷新，也可点击圆形刷新按钮。

## 8. 条件满足后，本机一键启动

前置条件：服务器已运行；本地 Web 包已解压，或源码的 `web/dist/index.html` 已构建；Windows Python 已安装。已保存连接后，不需要每次 SSH、构建或解压。

**Windows PowerShell：在源码根目录或 Web 解压目录执行一条命令：**

```powershell
powershell -ExecutionPolicy Bypass -File .\start-web.ps1
```

Windows 的一键入口是 `.ps1`。如果你习惯 **Git Bash 的 sh/bash 终端**，在相同目录执行以下一条命令即可，仍通过 Windows 启动器运行并保留 Credential Manager 支持：

```bash
powershell.exe -ExecutionPolicy Bypass -File ./start-web.ps1
```

这里的 bash 命令适用于 Windows Git Bash；不是在远端 SSH 中执行，也不是启动 Linux 版 Web。WSL 用于构建，日常 Web 请从 Windows 启动。

已经按第 6 节安装后，从任意 PowerShell 位置也可启动（版本号与已安装目录一致）：

```powershell
powershell -ExecutionPolicy Bypass -File (Join-Path $env:LOCALAPPDATA 'OpenCodeCloud\cloud-agent-web-0.3.0\start-web.ps1')
```

端口被占用时，在源码/解压目录启动其他端口：

```powershell
powershell -ExecutionPolicy Bypass -File .\start-web.ps1 -Port 18766
```

本地页面随之变为 `http://127.0.0.1:18766/chat`，服务器 URL 保持不变。按 `Ctrl+C` 停止本地服务不会停止远端 Agent 平台。

## 9. 常见问题与服务器维护

| 问题 | 处理方式 |
|---|---|
| 找不到 ZIP 或报告 | 检查 `$packageDir`；全新 clone 先构建，已有包应使用同批次报告 |
| ZIP 校验失败 | 确认 ZIP 和报告是同一构建，重新获取完整文件，不删除校验步骤 |
| 本地启动缺少页面 | 源码尚未构建前端，执行第 2 节或使用已编译的 Web 包 |
| `.env` 不存在/模型 key 为空 | 按第 3 节从模板创建并填写；仅启动 Web 不需要 `.env` |
| Web 返回 401 | 填写平台管理员令牌，不是模型 key；检查是否连接到正确服务器 |
| 远端就绪接口 503 | 查看服务日志和模型网关配置，等待初始化或修复依赖问题 |
| 远端本机能连，公网不能连 | 检查 18080 的入站规则、端口发布和反向代理 |
| Python/依赖安装失败 | 检查 Python 版本、PATH 和网络；修复后重跑启动器 |
| localhost 访问不了 | 保持 Web 终端运行，确认实际端口；第一次启动先等待依赖安装完成 |

以下命令在远端按需单独执行：

```bash
# 状态
sudo bash /srv/cloud-agent/deploy/status.sh --root /srv/cloud-agent
# 日志
sudo bash /srv/cloud-agent/deploy/logs.sh --root /srv/cloud-agent
# 启动已安装的平台
sudo bash /srv/cloud-agent/deploy/start.sh --root /srv/cloud-agent
# 停止平台，保留数据
sudo bash /srv/cloud-agent/deploy/stop.sh --root /srv/cloud-agent
```

本文命令供你执行，不表示已替你在远端部署。API 细节见 [API 契约](02_api_contract.md)。
