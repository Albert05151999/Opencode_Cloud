# 06：管理员自动压测

状态（2026-09-12）：06-01～06-10 源码及验证已完成，包含服务器健康/余量查询、准入和准备复查。最近结果为 286 项后端、18 项浏览器、9 项前端逻辑及真实两用户压测通过。最新功能尚未重新打包或部署生产，详见 [项目进度](项目进度.md)。

## 范围与默认行为

管理页增加“压测”。只能选择当前已发布且启用的 Agent；每个 Agent 设置模拟用户数、单沙箱 CPU 核数和内存 MiB。一个模拟用户对应一个新的 Agent × User 沙箱，一个会话，一次固定请求，使用 Agent 当前默认模型。测试不修改 Agent 的默认资源配置。

先准备所有测试会话，全部准备步骤结束后，已准备成功的用户同时发起同一个固定提示词。准备失败的用户也计入报告，不重复请求。报告分别记录准备耗时、并发发起时间差、成功率、请求耗时分位数、吞吐量、错误和每个用户/沙箱/会话标识。服务端发起请求，测试控制器、沙箱和模型链路，不声称测量浏览器或公网入口性能。

默认每个 Agent 1 个用户、1 CPU、1024 MiB；单任务最多 100 用户，准备并发 4，一次只运行一项压测。界面展示总沙箱数与资源配额合计。固定提示词不要求工具或权限；请求超时会尝试终止对应会话。支持取消，取消不自动删数据。进程重启后不重放模型请求，未完成任务标记中断。

测试结束保留报告与测试数据。管理员在报告中明确确认后销毁本次测试的沙箱、会话及用户工作区；清理可以重试，不删除 Agent、资源库或正常用户数据。测试用户使用独立随机标识并持久记录归属；清理中和清理后禁止再次唤醒这些用户。报告在清理后保留。

## TODO

| 状态 | 项目 | 产物与验收 |
|---|---|---|
| [x] | 06-01 任务、用户归属和资源 DTO | SQLite 独立表；参数、现有 Agent、幂等及任务并发校验 |
| [x] | 06-02 沙箱资源覆盖与隔离 | CPU/内存仅影响本次测试用户，重启后仍有效；不改变正常用户 |
| [x] | 06-03 自动执行与同步发起 | 准备会话、同时请求、超时/取消、中断恢复；每用户只提交一次 |
| [x] | 06-04 持久报告与 API | 总体及按 Agent 汇总、用户明细、JSON/CSV 下载 |
| [x] | 06-05 管理员清理 | 精确归属、忙碌保护、幂等重试、保留报告与正常数据 |
| [x] | 06-06 管理页面 | Agent 选择、用户/资源表单、进度、报告、取消和清理 |
| [x] | 06-07 验证与文档 | 单元测试、浏览器用例、两用户真实沙箱验证、API 与使用说明 |
| [x] | 06-08 宿主机健康与保守容量预算（依赖 06-02） | `app/load_capacity.py`；CPU 实测、实际内存、容器配额和系统预留；未知状态拒绝启动 |
| [x] | 06-09 启动准入与准备复查（依赖 06-08、06-03） | 创建用户前校验；每批准备及生成前复查；容量变化停止后续执行，保留报告和待清理数据 |
| [x] | 06-10 容量页面与验收（依赖 06-08、06-09） | 每 5 秒刷新、手动刷新、每 Agent 用户上限提示、超预算禁用；回归与真实 Docker 验证通过 |

## 接口

- `GET /cloud/admin/load-tests/options`：现有 Agent、默认限制与主机容量摘要。
- `GET /cloud/admin/load-tests/capacity`：CPU/内存状态、已分配配额、预留和可分配余量。
- `GET/POST /cloud/admin/load-tests`：任务列表与创建。
- `GET /cloud/admin/load-tests/{id}`：状态、进度与报告。
- `POST /cloud/admin/load-tests/{id}/cancel`：取消未完成请求。
- `POST /cloud/admin/load-tests/{id}/cleanup`：携带完整测试 ID 确认销毁。
- `GET /cloud/admin/load-tests/{id}/report?format=json|csv`：下载报告。

所有调用沿用现有管理员 API 与本地同源转发。执行器、报告计算、资源归属和页面分开封装；不新增服务或数据库。

## 完成记录（2026-09-11）

06-01～06-07 已完成。核心文件为 `app/load_test_store.py`、`app/load_tests.py`、`app/load_test_api.py` 和 `web/src/LoadTests.tsx`；沙箱创建仅为登记的测试用户覆盖 CPU/内存，并增加测试归属标签。配置发布等待压测结束；涉及当前测试 Agent 的归档和沙箱启停要求先停止压测。

| 检查 | 命令 / 证据 | 结果 |
|---|---|---|
| 后端完整回归 | `python -m pytest tests/unit -q` | 278 项通过，新增 17 项压测测试 |
| 前端编译 | `cd web` 后 `npm run build` | 通过；保留原有 bundle 体积提示 |
| 前端状态逻辑 | `npm test` | 9 项通过 |
| 浏览器回归 | `WEB_TEST_BASE_URL=http://127.0.0.1:18766 npx playwright test` | 17 项通过，含压测表单与清理确认；远端 API 为模拟 |
| 真实压测 | `python scripts/verify-load-testing.py` | 独立实例中已有 agent-code、2 用户、2 沙箱，每个 1 CPU / 1024 MiB；2 次请求均成功 |
| 真实资源与清理 | `artifacts/web/load-testing-verification.json` | Docker CPU/内存核对通过；测试目录与容器清除，正常用户文件与报告保留，旧测试用户无法再次创建会话 |

该次真实测试的发起时间差为 26 ms，成功请求耗时约 11.10～13.64 秒。这是当前测试宿主与模型网关的样本，不是生产容量结论。测试只清理自己实例标签下的容器。

使用说明见 `docs/load-testing.md`，公开接口已写入 `02_api_contract.md`。本次完成源码与前端构建，没有覆盖此前的 0.3.0 交付 ZIP；部署新增能力时需要重新构建服务器与 Web 包。

## 容量检查完成记录（2026-09-12）

06-08～06-10 已完成。策略封装在 `LoadCapacityConfig` 与 `LoadCapacity`，外围只需修改 `config.cfg` 的可选 `[load_capacity]` 节。旧配置自动使用默认预留规则。预算包括其他运行容器及可重启的已停止沙箱；实际内存扣除未使用配额的未来增长空间。超过预算或监测未知时拒绝压测，不能通过直接请求 API 绕过。

| 检查 | 命令 / 证据 | 结果 |
|---|---|---|
| 后端完整回归 | `python -m pytest -q` | 286 项通过；新增 8 项覆盖预算、停止容器、未知/过期、配置兼容、幂等、准备中途容量下降 |
| 前端构建 | `cd web` 后 `npm run build` | 通过；原有 bundle 体积提示仍存在 |
| 前端逻辑 | `npm test` | 9 项通过 |
| 浏览器回归 | `WEB_TEST_BASE_URL=http://127.0.0.1:18766 npx playwright test` | 18 项通过；新增预算不足及监测未知的页面用例，远端 API 使用模拟 |
| 真实 Docker 与模型调用 | `python scripts/verify-load-testing.py` | 读取真实容量；超预算拒绝且不创建测试用户；两用户独立沙箱、模型请求、配额核验与清理通过 |

本次真实验证宿主为 16 核、约 15638 MiB；采样时预留 1.6 核和约 1564 MiB，可分配 14.4 核和 13044 MiB。这是开发验证环境的瞬时结果，不代表远端生产服务器容量。最新证据写入 `artifacts/web/load-testing-verification.json`，替换上次验证文件。源码及编译页面已更新，旧交付 ZIP 未覆盖，部署时需重新构建两端包。
